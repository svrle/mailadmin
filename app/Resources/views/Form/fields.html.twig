{% extends 'form_div_layout.html.twig' %}

{%- block form_widget_simple -%}
{#{%- block property_widget -%}#}
    {% spaceless %}
        {%- set type = type|default('text') -%}
        {% if property_value is defined %}
            {% if property_value is not null %}
                <a name="{{ property_name }} "></a>
                <label>{{ property_name|humanize }} <br \><h6>({{ property_description }})</h6></label>
                <input type="{{ type }}" {{ block('widget_attributes') }} {% if property_value is not empty %}value="{{ property_value }}" {% endif %}/>
            {% endif %}
        {% else %}
            {{ parent() }}
            {#<input type="{{ type }}" {{ block('widget_attributes') }} {% if value is not empty %}value="{{ value }}" {% endif %}/>#}
        {% endif %}
    {% endspaceless %}
{%- endblock form_widget_simple -%}
{#{%- endblock property_widget -%}#}

{#{%- block form_widget_simple -%}#}
    {#{%- set type = type|default('text') -%}#}
    {#<input type="{{ type }}" {{ block('widget_attributes') }} {% if value is not empty %}value="{{ value }}" {% endif %}/>#}
{#{%- endblock form_widget_simple -%}#}